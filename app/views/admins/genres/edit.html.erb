<div class="container">
 <div class="row">
  <h4>ジャンル編集</h4>
  <div>
   <%= form_with model: @genre, url:admins_genre_path(@genre.id), method: :patch do |f| %>
   <h6>ジャンル名</h6>
   <%= f.text_field :name %>
   <%= f.submit '変更を保存' %>
   <% end %>
  </div>
 </div>
</div>